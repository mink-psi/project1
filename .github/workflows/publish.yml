name: Publish

on:
  workflow_call:
    inputs:
      environment:
        description: 'Hosting environment'
        required: true
        type: string

jobs:
  publish:
    uses: mink-psi/.github/workflows/publish.yml@main
